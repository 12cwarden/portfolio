{
  "name": "portfolio",
  "version": "2.0.0",
  "private": true,
  "description": "Cory Warden | Developer Portfolio",
  "source": "./index.html",
  "scripts": {
    "start": "parcel",
    "build": "parcel build --public-url ./",
    "preview": "npx serve dist",
    "clean": "rimraf .parcel-cache dist",
    "clean:start": "npm run clean && parcel",
    "clean:build": "npm run clean && parcel build --public-url ./"
  },
  "author": "Cory Warden",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.16.4",
    "parcel": "^2.16.4",
    "prettier": "^2.4.1",
    "rimraf": "^6.1.3"
  },
  "dependencies": {
    "@popperjs/core": "^2.10.2",
    "bootstrap": "^5.1.3",
    "scrollreveal": "^4.0.9",
    "vanilla-tilt": "^1.7.2"
  }
}
